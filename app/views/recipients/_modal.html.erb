<div class="modal" id="RecipientAddModal" tabindex="-1" role="dialog" aria-labelledby="RecipientAddModalLabel">

  <div class="modal-dialog modal-lg modal-dialog-centered cgroup-pop-modal" role="document">
    <div class="modal-content">


      <div class="modal-body">
        <div class="cgroup-pop">
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">×</span>
          </button>
          <div class="cgroup-pop-header text-center">
            <div class="cgroup-pop-header-title"><%= @recipient.new_record? ? "#{t 'lists.create_new_list'}" : "#{t 'lists.update'}" %> <%=t 'lists.recipient'%></div>
            <div class="cgroup-pop-header-text"><%=t 'lists.your_about_to'%> <%= @recipient.new_record? ? "#{t 'lists.create_new_list'}" : "#{t 'lists.update'}" %> <%=t 'lists.recipient'%>. <%=t 'lists.please_give_your'%> <br><%=t 'lists.recipient_a_name'%></div>
          </div>

          <%= render 'form', recipient: @recipient %>

        </div>
      </div>

    </div>
  </div>

</div>

<script type="text/javascript" charset="utf-8">
    $('#RecipientAddModal').modal();
</script>




